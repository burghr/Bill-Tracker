SESSION_SECRET=change-me-to-a-random-secret

# Auth mode: "local" for built-in login, "sso" for Authentik forward auth
AUTH_MODE=local
#SSO_HEADER_USERNAME=X-authentik-username
#SSO_HEADER_EMAIL=X-authentik-email
